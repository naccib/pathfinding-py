[project]
name = "tests"
version = "0.1.0"
description = "Integration tests for pathfinding_py"
requires-python = ">=3.8"
dependencies = ["numpy>=1.20.0", "pytest>=7.0.0", "pillow>=9.0.0"]

[tool.pytest.ini_options]
testpaths = ["."]
python_files = ["test_*.py"]
